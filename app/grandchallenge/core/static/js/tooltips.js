// Bootstrap tooltips are opt-in
// Set attribute 'data-toggle' to "tooltip" on elements that should have a tooltip
// For example:
//   <div data-toggle="tooltip" title="Tooltip content"></div>

$(document).ready(() => {
    $('[data-toggle="tooltip"]').tooltip();
});
